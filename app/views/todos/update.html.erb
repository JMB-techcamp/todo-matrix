<% todolist = current_user.todos.order('importance ASC')%>
<header>
  <div class="title"><h1>重要度を選択してください。</h1></div>
  <div class="icon"><a href="/users/mypage" class="btn"><i class="far fa-check-circle fa-5x"></i></a><div class="icon-text">決定</div></div>
</header><!-- /header -->

<div class="todo-table">
  <table>
    <thead>
      <tr>
        <th>タスク</th>
        <th>期限</th>
      </tr>
    </thead>
    <tbody id="sortable">
      <%  todolist.each do |todo| %>
        <% if todo.id == @todo.id %>
          <tr id="row_<%= todo.id %>" >
            <td class="newtodo"><%= todo.title %></td>
            <td class="newtodo"><%= todo.dead_line.strftime("%Y/%m/%d %H:%M") %></td>
            <!-- <td class="newicon"><i class="fas fa-arrows-alt-v fa-2x"></i></td> -->
          </tr>
        <% else %>
          <tr id="row_<%= todo.id %>">
            <td><%= todo.title %></td>
            <td><%= todo.dead_line.strftime("%Y/%m/%d %H:%M") %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
<div class="todo-table">

<script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/2.2.3/jquery.js'></script>
<link rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/jqueryui/1.12.0/jquery-ui.css' />
<script src='https://cdnjs.cloudflare.com/ajax/libs/jqueryui/1.12.0/jquery-ui.min.js'></script>
<script src='https://cdnjs.cloudflare.com/ajax/libs/jqueryui-touch-punch/0.2.3/jquery.ui.touch-punch.min.js'></script>

<script type="text/javascript">
$(function() {
  $("#sortable").sortable({
     cursor: 'move',
     start: function(event, ui) {ui.item.css({'background-color':'rgba(255,0,0,0.1)', 'font-size':'30px'});},
     stop: function(event, ui) {
       ui.item.css({'background-color':'rgba(255,0,0,0.1)', 'font-size':'30px'});
       jQuery.post('<%=importance_todos_path%>', $(this).sortable('serialize'), null, 'text');
     }
  });
  $("#sortable").disableSelection();
});
</script>
